<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Settings, should always be in the overflow -->
    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/action_settings_foreground"
        android:title="@string/action_settings"
        app:showAsAction="never" />
    <item
        android:id="@+id/info_app"
        android:icon="@drawable/info_set_icon_foreground"
        android:title="@string/info_app" />

    <item
        android:id="@+id/logout_app"
        android:icon="@drawable/info_set_icon_foreground"
        android:title="@string/logoutText" />
</menu>